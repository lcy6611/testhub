# ===== TestHub Production (HTTP + existing MySQL) =====
#
# 说明：
# - 出于安全策略，仓库里不直接放 .env 文件名。
# - 部署时请复制为 .env.prod（与 docker-compose.prod.yml 中 env_file 对应）。
#
#   Windows:
#     copy env.prod.example.txt .env.prod
#
#   Linux/macOS:
#     cp env.prod.example.txt .env.prod

# Django
DEBUG=false
SECRET_KEY=change-me-to-a-long-random-string
ALLOWED_HOSTS=192.168.56.1

# MySQL（现有 MySQL）
DB_HOST=your.mysql.host
DB_PORT=3306
DB_NAME=testhub
DB_USER=root
DB_PASSWORD=change-me

# Redis
REDIS_PASSWORD=change-me

# Nginx
HTTP_PORT=80

# 可选：生产环境 CORS（如果你是同域 Nginx 统一入口，一般不需要动）
# CORS_ALLOWED_ORIGINS=http://192.168.56.1

